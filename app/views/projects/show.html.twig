{% extends 'partials/base.html.twig' %}
{% block content %}
    <p>{{ project.title }}</p>
    <p>{{ project.description }}</p>
    <br>

    <h1>Taks</h1>
    {% for task in tasks %}
        <p>{{ task.title }}</p>
    {% endfor %}
{% endblock %}